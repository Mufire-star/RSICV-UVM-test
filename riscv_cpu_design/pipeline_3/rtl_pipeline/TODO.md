# rtl_pipeline TODO

- [ ] 将 `IM` 替换为 ROM 或 SRAM wrapper，并按真实程序容量缩小深度。
- [ ] 将 `DM` 替换为 SRAM wrapper，并按真实数据容量缩小深度。
- [ ] 将 `RF` 替换为专用 `2R1W` 寄存器堆 wrapper，或改成更利于映射 macro 的实现。
- [ ] 明确真实 `IM/DM` 的读写时序模型，并据此校正顶层数据通路。
- [x] 在 `ALU` 内显式让 `add` 和 `sub` 复用同一个加法器。
- [x] 将数据通路改成三级流水，并让 `DM` 功能模型按同步读写方式工作。
- [x] 增加 `WB -> EX` 前递路径，用于消除大部分相邻数据相关。
- [ ] 保留一个小型专用 `PC + 4` 递增器，而不是强行复用主 ALU。
- [ ] 为 `branch`、`jal`、`jalr` 共享一条跳转目标地址加法路径。
- [ ] 从 `IF_ID` 中移除 `pc4_out`，仅在写回真正需要时再计算 `pc + 4`。
- [x] 移除独立的占位 `HazardUnit`，将当前固定逻辑直接并回顶层。
- [ ] 在综合前继续清理未使用的控制信号。
- [x] 为 `IM/DM/RF` 增加 `TECH` 占位版本和独立编译检查入口。
- [ ] 在完成时序分析后，重新评估 `beq/bne` 是保留独立比较器还是复用 ALU 的减法路径。
